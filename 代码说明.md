# 智能值班助手 Agent — 代码说明

> 面向华为云 HCS（Huawei Cloud Stack）的智能值班助手。通过 AI Agent 实现**自动化问题分类、知识检索、解决方案推荐**三大能力。
>
> 本文档从架构到每个文件，逐层说明代码的内容与作用。使用说明见 [使用手册.md](使用手册.md)。

---

## 一、项目概述

本项目是一个 **RAG + Agent** 问答系统：

- **知识来源**：`docs/` 下的《GaussDB V2.0-25.860.0 参考指南》（华为云 Stack 8.6.0），已向量化进 FAISS 索引
- **工作方式**：用户提问 → LLM 判断是否需要检索 → 需要则改写查询并多路检索（语义 + 关键词）→ Rerank 精排 → 组装上下文 prompt → 生成答案
- **运行形态**：Streamlit Web 聊天界面 / FastAPI 服务 / 命令行测试脚本

**三大能力落点：**

| 能力 | 实现位置 |
| --- | --- |
| 自动化问题分类 | `generate_answer` 的生成 prompt 内嵌分类指令（错误码/日志路径/操作指南） |
| 知识检索 | `src/rag/`：FAISS 语义 + BM25 关键词 + CrossEncoder Rerank 多路召回 |
| 解决方案推荐 | LLM 基于检索到的参考资料整合生成（含错误描述/原因/建议） |

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────┐
│  UI / 接入层                                             │
│   Streamlit 聊天界面    FastAPI 服务    CLI 测试脚本       │
└───────────────────────────┬─────────────────────────────┘
                            │ agent.invoke(state)
┌───────────────────────────▼─────────────────────────────┐
│  Agent 层（LangGraph 有向图）                             │
│  analyze ──┬─(有工具调用)──▶ rewrite_query ─▶ execute_tools │
│            │                 │                             │
│            └─(无工具调用)────┴─────────────▶ generate      │
└───────────────────────────┬─────────────────────────────┘
                            │ 检索 / 工具调用
┌───────────────────────────▼─────────────────────────────┐
│  RAG 检索层                                               │
│  文档加载 → 文本切分 → 向量化(FAISS) + 关键词(BM25)        │
│            → 多路融合(Ensemble) → Rerank 精排             │
└─────────────────────────────────────────────────────────┘
```

---

## 三、目录结构

```
智能值班助手Agent/
├── config.py                      # 全局配置（API Key、路径、检索/重写参数）
├── requirements.txt               # Python 依赖清单
├── .gitignore                     # Git 忽略清单（__pycache__、.env、虚拟环境、索引等）
├── 使用手册.md                     # 使用手册（本项目的使用说明）
├── 代码说明.md                     # 本文档（代码结构说明）
│
├── src/                           # 核心代码
│   ├── rag/                       # RAG 检索层
│   │   ├── __init__.py            #   包标识（空）
│   │   ├── loader.py              #   文档加载器（PDF/Word/TXT）
│   │   ├── splitter.py            #   文本切分器
│   │   ├── embeddings.py          #   嵌入模型加载
│   │   ├── retriever.py           #   检索器：FAISS + BM25 + Ensemble + Rerank
│   │   └── query_rewriter.py      #   查询重写器
│   ├── agent/                     # Agent 层
│   │   ├── __init__.py            #   包标识（空）
│   │   ├── state.py               #   LangGraph 状态定义（AgentState）
│   │   ├── tools.py               #   工具定义（RAG检索/天气/时间）
│   │   ├── nodes.py               #   图节点逻辑（4 个节点）
│   │   └── graph.py               #   图构建（LangGraph 编排）
│   ├── ui/                        # 界面层
│   │   ├── __init__.py            #   包标识（空）
│   │   └── streamlit_app.py       #   Streamlit 聊天界面（主入口）
│   └── __init__.py                #   包标识（空）
│
├── scripts/                       # 工具脚本
│   ├── build_index.py             #   构建 FAISS 向量索引
│   ├── test_rewrite.py            #   查询重写端到端测试（CLI）
│   └── evaluate.py                #   评测脚本（三变体消融；配套 eval_set.json 评测集）
│
├── 工程化和部署/                    # 部署入口
│   ├── api.py                     #   FastAPI 服务（正式 API）
│   └── app.py                     #   Streamlit 简单版（纯 RAG，无 Agent）
│
├── docs/                          # 文档库（放 PDF/Word/TXT，检索的知识来源）
├── data/faiss_index/              # FAISS 向量索引（随仓库携带，供云端加载）
└── .streamlit/                    # Streamlit 服务端配置 + Secrets 模板
```

---

## 四、各模块详解

### 4.1 配置层 `config.py`

集中管理全部配置，导入即生效。

| 配置 | 值 | 作用 |
| --- | --- | --- |
| `BASE_DIR` | 项目根目录 | 自动计算 |
| `DOCS_DIR` | `<根>/docs` | 文档库目录 |
| `INDEX_SAVE_PATH` | `F:/faiss_index_data`（可被 `FAISS_INDEX_PATH` 覆盖） | FAISS 索引路径（**必须纯 ASCII**；云端用环境变量指定，如 `/app/data/faiss_index`） |
| `DEEPSEEK_API_KEY` | 只从环境变量读取，未设置打印警告 | DeepSeek API Key（本地用 `set`/`$env:` 设置，云端用 Secrets 注入；**代码中不保存明文 Key**） |
| `LLM_BASE_URL` | `https://api.deepseek.com/v1`（可被环境变量覆盖） | 生成 LLM 的 OpenAI 兼容接口地址（换供应商改这里） |
| `LLM_MODEL` | `deepseek-chat`（可被环境变量覆盖） | 生成 LLM 的模型名（换供应商改这里） |
| `EMBEDDING_MODEL` | `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` | 嵌入模型（384 维，支持中文） |
| `RERANK_MODEL` | `cross-encoder/ms-marco-MiniLM-L-6-v2` | Rerank 精排模型 |
| `CACHE_DIR` | `~/.cache/huggingface/hub`（可被 `HF_CACHE_DIR` 覆盖） | HuggingFace 模型缓存目录（跨平台） |
| `CHUNK_SIZE` / `CHUNK_OVERLAP` | `2500` / `300` | 文本切块大小与重叠 |
| `TOP_K_FIRST` / `TOP_K_FINAL` | `50` / `15` | FAISS 粗筛数 / Rerank 最终保留数 |
| `BM25_K` | `50` | BM25 返回候选数 |
| `ENSEMBLE_WEIGHTS` | `[0.5, 0.5]` | 语义检索与关键词检索的融合权重 |
| `TEMPERATURE` | `0.3` | 生成答案的温度 |
| `QUERY_REWRITE_ENABLED` | `True` | 是否开启查询重写 |
| `QUERY_REWRITE_COUNT` | `2` | 每个问题改写为几个查询（调小可降低回答等待时间） |
| `QUERY_REWRITE_TEMPERATURE` | `0.1` | 改写用低温度（更稳定） |
| `QUERY_REWRITE_TOP_K` | `20` | 每个改写查询的候选数（2×20=40 → 去重 → 精排取15） |

文件末尾按环境变量配置模型下载：默认**离线**（`HF_OFFLINE=1`，用本地缓存）；云端设 `HF_OFFLINE=0` 联网下载；`HF_ENDPOINT` 默认官方源 `huggingface.co`（云端境外服务器连不上 hf-mirror），国内本地联网下载可覆盖为 `hf-mirror.com`。

---

### 4.2 RAG 检索层 `src/rag/`

#### `loader.py` — 文档加载器
遍历 `docs/` 目录，按扩展名自动选择加载器：
- `.pdf` → `PyPDFLoader`
- `.docx` → `Docx2txtLoader`
- `.txt` → `TextLoader`

返回 `Document` 列表（每个 PDF 页是一个片段，元数据含 `source` 与 `page`）。

#### `splitter.py` — 文本切分器
用 `RecursiveCharacterTextSplitter` 按段落 → 换行 → 句号 → 分号 → 逗号 → 空格 → 字符 的优先级切分，防止边界信息丢失。

#### `embeddings.py` — 嵌入模型加载
- `get_embeddings()`：返回 `HuggingFaceEmbeddings` 实例，使用多语言 MiniLM 模型，CPU 运行、归一化向量；`local_files_only` **跟随离线开关 `config.HF_OFFLINE`**（本机默认离线用本地缓存；云端 `HF_OFFLINE=0` 时允许联网下载模型）
- `probe_hf_endpoint(model_id)` / `probe_and_report(model_id)`：**云端排障工具**，加载模型前探测 `huggingface.co` / `hf-mirror.com` 的真实下载路径连通性并打进日志（`[网络诊断]` 行，本地离线模式跳过）

#### `retriever.py` — 检索核心（最关键）
| 函数 | 作用 |
| --- | --- |
| `chinese_tokenizer(text)` | **BM25 中文分词器**：用 jieba `cut_for_search` 分词，并额外保留错误码串（如 `DBS.200026`）。BM25 默认按空格分词对中文失效，此函数是中文关键词检索生效的关键 |
| `load_vectorstore()` | 从 `INDEX_SAVE_PATH` 加载 FAISS 向量库（`allow_dangerous_deserialization=True`，本地可信文件） |
| `create_bm25_retriever(chunks)` | 从文档块创建 BM25 检索器，**显式传入 `chinese_tokenizer`** |
| `create_ensemble_retriever(faiss, bm25)` | 用 `EnsembleRetriever` 融合两路召回（权重取自 `ENSEMBLE_WEIGHTS`），内部按 RRF 重排 |
| `load_reranker()` | 加载 CrossEncoder 精排模型（`local_files_only` 跟随 `HF_OFFLINE`，同样带 `[网络诊断]` 探测） |
| `retrieve_with_rerank(query, retriever, reranker, top_k)` | 完整检索：召回 → 截断到 `top_k` → Rerank → 取 `TOP_K_FINAL` 个 |
| `format_docs(docs)` | 把文档列表拼成上下文字符串 |
| `load_retrieval_components()` | **一键加载整套检索组件**（Streamlit / FastAPI / 测试脚本共用，避免重复代码）：加载 FAISS 向量库 → 创建语义检索器 → 加载 Rerank → 加载文档构建 BM25，返回 `(vector_store, faiss_retriever, bm25_retriever, reranker)` |

> ⚠️ 注意：`EnsembleRetriever` 的导入按 langchain 版本自适应——`langchain 1.x` 走 `langchain_classic.retrievers`，`langchain 0.3.x` 走 `langchain.retrievers`。

#### `query_rewriter.py` — 查询重写器
`rewrite_queries(llm, question, history_text, count)`：
1. 让 LLM 把口语化问题改写成多个检索友好的查询（提取关键词/错误码/补充同义词）
2. 解析输出、去重、过滤掉与原始问题相同的变体
3. 任何异常或空结果都回退为 `[原始问题]`，不影响主流程

---

### 4.3 Agent 层 `src/agent/`

#### `state.py` — 状态定义
`AgentState`（`TypedDict`），在节点间传递的字段：

| 字段 | 含义 |
| --- | --- |
| `question` | 当前用户问题 |
| `messages` | 对话历史（用于多轮记忆） |
| `retrieved_docs` | 检索到的文档内容（格式化字符串） |
| `sources` | 来源列表 `List[str]` |
| `answer` | 最终答案（**不走工具时**由 analyze 直接给出；走工具时由 UI/API 层补全） |
| `prompt` | 传给 UI/API 层做流式生成的 prompt |
| `rewritten_queries` | 查询重写生成的多个检索查询 |

#### `tools.py` — 工具定义
| 工具 | 作用 |
| --- | --- |
| `create_rag_tool(retriever, reranker, top_k)` | 工厂函数，生成 `rag_search(query)` 工具。`top_k=None` 时用 `TOP_K_FIRST`；多查询场景传 `QUERY_REWRITE_TOP_K` |
| `get_weather(city)` | 查询实时天气（wttr.in），失败返回模拟数据 |
| `get_current_time()` | 获取当前日期时间 |

#### `nodes.py` — 图节点逻辑（4 个节点）

**Node 1 `analyze_question(state, llm_with_tools)`** — LLM 自主决策

- 每轮开始时**清空上一轮**的 `retrieved_docs` / `sources` / `rewritten_queries` / `answer`（避免陈旧上下文串扰）
- 防御性过滤历史中的 system 消息
- 带系统提示词 + 历史 + 当前问题调用 LLM（已绑定工具）
- **关键**：只保存干净对话历史（不含 system），避免多轮后提示词爆炸式累积
- 返回：有 `tool_calls` 则进入工具路径；否则把 `response.content` 写入 `answer` 直接回答

**Node 2 `rewrite_query_node(state, llm)`** — 查询重写
- 配置关闭 → 直接用原始问题
- 开启 → 调 `rewrite_queries` 生成多个查询，存入 `rewritten_queries`；异常兜底为原始问题

**Node 3 `execute_tools(state, rag_tool, rag_tool_multi, weather, time)`** — 执行工具
- 按 `tool_calls` 逐个执行；识别 `rag_search`、`get_weather`、`get_current_time`
- `rag_search` 支持**多查询**：若 `rewritten_queries` 有多个不同查询，用 `rag_tool_multi`（`QUERY_REWRITE_TOP_K` 候选）逐个检索，再**按来源去重合并**成一份上下文
- 每个工具结果追加 `ToolMessage`，来源聚合去重后统一写入 `sources`

**Node 4 `generate_answer(state, llm)`** — 生成 prompt（不立即调 LLM）
- 有 `retrieved_docs`：拼"对话历史 + 参考资料 + 当前问题"的完整 prompt，内嵌**问题分类**指令（错误码→错误描述/原因/建议）
- 无检索结果：拼普通对话 prompt
- 只写入 `state["prompt"]` 返回，**实际生成由 UI 层流式调用 / API 层调用完成**（答语双轨设计的约定点）

#### `graph.py` — 图构建
`build_agent(faiss_retriever, bm25_retriever, reranker)`：
1. 创建 Ensemble 多路召回检索器
2. 创建 4 个工具（`rag_tool` / `rag_tool_multi` / 天气 / 时间）
3. 创建两个 LLM：`llm_with_tools`（analyze 用，绑定工具）+ `llm_no_tools`（重写/生成用）。两者的 `base_url`/`model` 读 `config.LLM_BASE_URL` / `config.LLM_MODEL`（换供应商改配置即可，见使用手册 4.5 节）
4. 图流程：

```
analyze（入口）
   │  条件边
   ├─ 有 tool_calls ─▶ rewrite_query ─▶ execute_tools ─┐
   │                                                  ▼
   └─ 无 tool_calls ───────────────────────────────▶ generate ─▶ END
```

---

### 4.4 界面层 `src/ui/streamlit_app.py`

Streamlit 聊天界面（主入口，`streamlit run src/ui/streamlit_app.py`）：

| 功能 | 说明 |
| --- | --- |
| `load_rag_system()` | 启动时加载 FAISS 索引 + Rerank 模型 + 重建 BM25 索引 + 构建 Agent（`@st.cache_resource` 只加载一次） |
| 聊天界面 | `st.chat_message` 展示历史与实时流式输出 |
| **流式生成** | 走工具路径（有 prompt 无 answer）时，UI 层用 `llm.stream(prompt)` 逐字渲染（`base_url`/`model` 同样读 `config.LLM_BASE_URL` / `config.LLM_MODEL`） |
| **直接回答** | 不走工具路径时直接用 analyze 生成的 `answer`（避免二次调用 LLM） |
| **多轮记忆** | 流式生成的真实回答会追加进 `agent_state["messages"]`，下一轮 LLM 可看到（否则只有空的工具调用消息） |
| 来源展示 | `📄 查看引用来源` expander 列出 `sources` |
| 调试信息 | 侧边栏显示查询重写结果、来源数量、历史消息数等 |
| 重新加载 | 清空缓存重载模型；清空对话重置会话 |

> **环境与诊断**：入口顶部按 `HF_OFFLINE` 设置离线开关（本地默认离线、云端 `HF_OFFLINE=0` 联网下载模型），`HF_ENDPOINT` 默认官方源 `huggingface.co`；启动时打印 `[启动诊断]` 行，云端排障可直接从 Logs 确认运行环境（`HF_OFFLINE` / `HF_ENDPOINT` / `DEEPSEEK_API_KEY` 是否注入）。

### 4.5 部署层 `工程化和部署/`

#### `api.py` — FastAPI 服务（正式 API）
- 启动时加载一次 Agent（`on_event("startup")`）
- 接口：
  - `GET /` — 服务信息
  - `GET /health` — 健康检查
  - `POST /ask` — 问答接口，入参 `{question}`，返回 `{answer, sources, used_tool}`
- **关键**：走工具路径时 Agent 只产出 prompt，`/ask` 在这里调用 LLM 从 `prompt` 补全 `answer`（否则会返回空答案）；该 LLM 的 `base_url`/`model` 读 `config.LLM_BASE_URL` / `config.LLM_MODEL`

#### `app.py` — Streamlit 简单版
纯 RAG 链（无 Agent、无多工具、无查询重写），`FAISS 粗筛 → Rerank → LLM`，用于快速测试检索效果；侧边栏展示的生成模型名来自 `config.LLM_MODEL`。

> `api.py` / `app.py` 与主界面共享相同的环境处理：按 `HF_OFFLINE` 设置离线开关、`HF_ENDPOINT` 默认官方源、启动打印 `[启动诊断]`；LLM 同样统一读 `config.LLM_BASE_URL` / `config.LLM_MODEL`。

### 4.6 工具脚本 `scripts/`

| 脚本 | 用途 |
| --- | --- |
| `build_index.py` | 加载 `docs/` 文档 → 切分 → 向量化 → 保存到 `INDEX_SAVE_PATH`。**新增/修改文档后必须重跑**。加 `--sync-repo` 参数时，构建后还会把索引复制到仓库内 `data/faiss_index`（供云端加载，提交推送后生效） |
| `test_rewrite.py` | CLI 端到端测试：提问"值班时遇到错误码 DBS.200026 怎么处理？"，打印改写查询、来源、prompt，验证查询重写链路 |
| `evaluate.py` | 三变体消融评测：对照 `eval_set.json` 40 题（错误码/日志路径/操作指南/路由），分别跑 baseline / no_rewrite / no_bm25 三配置，输出检索命中率与回答正确率对比表；结果增量写 `data/eval_results.json` |

---

## 五、核心数据流

### 5.1 走工具路径（检索/天气/时间）
```
用户问题
  → analyze: LLM 判定需工具，返回 tool_calls，存干净历史
  → rewrite_query: 生成 N 个改写查询（配置开启时）
  → execute_tools: 多查询逐条检索 → 按来源去重合并 → 追加 ToolMessage
  → generate: 组装【参考资料 + 分类指令】的 prompt 存入 state
  → UI/API: 用 prompt 调 LLM 生成最终答案（含问题分类与来源）
```

### 5.2 不走工具路径（闲聊/常识）
```
用户问题
  → analyze: LLM 直接给出回答，写入 state["answer"]，存干净历史
  → generate: 组装普通对话 prompt（不覆盖 answer）
  → UI: 直接展示 answer（不再二次调 LLM）
```

### 5.3 多轮对话如何工作
- 每轮 `state["messages"]` 追加 `[用户问题, LLM 回复]`（走工具时还有 ToolMessage）
- 下一轮 analyze 把 `messages` 作为历史喂给 LLM，实现上下文记忆
- 已修复多轮相关问题：system 提示词不再累积；真实回答写回历史；每轮清空旧答案（不再复读第一个回答）

---

## 六、关键设计要点与历史修复

1. **BM25 中文分词**（重要修复）：langchain-community 的 `BM25Retriever` 默认 `text.split()` 按空格分词，对中文整句失效。`create_bm25_retriever` 显式传入 `chinese_tokenizer`（jieba + 错误码串保留）修复。依赖 `jieba`。
2. **多轮 system 累积**（修复）：`analyze_question` 只保存不含 system 的干净历史。
3. **多轮记忆丢失**（修复）：Streamlit 把流式生成的真实回答 `AIMessage` 追加进 `agent_state["messages"]`。
4. **陈旧检索串扰 + 复读旧回答**（修复）：analyze 每轮清空上一轮检索结果与 `answer`，避免陈旧上下文串扰、避免 UI 复读第一个问题的回答。
5. **API 空答案**（修复）：`/ask` 走工具路径时由 API 层从 prompt 补全答案。
6. **答语双轨**（设计约定）：`answer`（analyze 直出）与 `prompt`（走工具后流式生成）二选一，UI/API 两入口都遵守同一约定。
7. **Streamlit stdout 关闭错误**（修复）：不在 Streamlit 里用 `TextIOWrapper` 替换 `sys.stdout`/`sys.stderr`（每次 rerun 重跑都会回收旧 wrapper 并关闭底层 buffer，导致 `I/O operation on closed file`），改为原位 `reconfigure(errors='replace')`。
8. **回答卡顿调优**：`QUERY_REWRITE_COUNT` 3→2、`QUERY_REWRITE_TOP_K` 30→20，本地 Rerank 量约减半。
9. **云端模型下载失败**（修复）：模型加载写死 `local_files_only=True` 会禁止联网下载，云端缓存为空必然失败；改为跟随 `HF_OFFLINE`（云端设 `HF_OFFLINE=0` 即联网下载）。
10. **云端下载源连不上**（修复）：入口脚本曾写死 `HF_ENDPOINT=hf-mirror`（境外服务器连不上国内镜像）；统一默认官方源 `huggingface.co`，并用 `setdefault` 尊重 Secrets 覆盖。
11. **云端排障工具**：启动打印 `[启动诊断]`（环境变量注入状态）、模型加载前打印 `[网络诊断]`（下载源连通性），Logs 一眼定位部署环境问题。
12. **LLM 配置收敛**（重构）：`base_url`/`model` 原硬编码在 4 个文件 6 处（graph.py×2、streamlit_app.py、api.py、app.py），收敛到 config.py 的 `LLM_BASE_URL` / `LLM_MODEL`（可环境变量覆盖），换模型只改配置、不用改代码。
13. **云端索引同步**（新增）：`build_index.py` 加 `--sync-repo`，本机构建后一键把索引复制到仓库内 `data/faiss_index`（用 `shutil.copy2` 复制，避开 FAISS 不支持中文路径的限制）。
