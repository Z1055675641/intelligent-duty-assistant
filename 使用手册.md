# 智能值班助手 Agent — 使用手册

> 面向**华为云 HCS（Huawei Cloud Stack）** 的智能值班助手智能体。
> 核心目标：通过 AI Agent 实现**自动化问题分类、知识检索、解决方案推荐**。
>
> 代码结构说明见 [代码说明.md](代码说明.md)。

---

## 一、项目简介

基于 **LangGraph Agent + 多路召回 RAG + DeepSeek** 的智能问答系统。用户提问后，系统自动判断是否需要检索知识库，改写查询、召回相关内容、精排、并结合大模型生成带分类与来源的解决方案。

**核心能力：**

| 功能 | 说明 |
| --- | --- |
| 🔄 查询重写 | 口语化问题自动改写成多个检索查询，提高召回率 |
| 📄 文档检索 | FAISS 语义检索，从华为云 HCS 文档库召回相关内容 |
| 🔑 关键词匹配 | BM25 精确匹配错误码（如 `DBS.200026`）、日志路径等 |
| 🎯 Rerank 精排 | CrossEncoder 对召回结果二次排序，保留最相关文档 |
| 🧠 自动化问题分类 | 生成时对问题分类（错误码/日志路径/操作指南），并按类组织答案 |
| 💡 解决方案推荐 | 针对错误码输出**错误描述、可能原因、解决建议** |
| 🛠 多工具 | RAG 检索 + 天气 + 时间 |
| 💬 多轮记忆 | 记住上下文，支持连续对话 |
| 🌐 Web 界面 | Streamlit 聊天式界面（流式输出） |
| 🔌 API 服务 | FastAPI 提供 REST 接口 |

---

## 二、系统架构

```text
提问 ─▶ analyze（LLM 决策）─┬─有工具调用─▶ 查询重写 ─▶ 多路检索 ─▶ Rerank 精排
                           │                （FAISS+BM25）        │
                           └─无工具调用─┐                        ▼
                                       └──────────▶ 组装 prompt ─▶ 生成答案（含分类+来源）
```

知识库：`docs/` 目录下的《GaussDB V2.0-25.860.0 参考指南》（已向量化进 FAISS 索引，共 370 个文本块）。

---

## 三、环境要求

- Python 3.9+（本机实测 3.9.13）
- Windows / Linux / macOS
- 能访问 DeepSeek API（`api.deepseek.com`）
- 约 2GB 磁盘空间（HuggingFace 模型缓存）

**已安装依赖版本（本机实测）：** langchain 0.3.x、langgraph、sentence-transformers 5.1.2、faiss-cpu、jieba、streamlit、fastapi、uvicorn。

---

## 四、安装与配置

### 4.1 安装依赖

```bash
pip install -r requirements.txt
```

> `requirements.txt` 已包含 `jieba`、`rank_bm25`、`fastapi`、`uvicorn` 等全部运行依赖，`pip install -r requirements.txt` 一次装齐。

### 4.2 配置 API Key（不修改 config.py）

`config.py` **只从环境变量读取**配置，**不保存明文 Key**——仓库是公开的，把 Key 写进代码会泄露给别人滥用。设置环境变量即可：

**方式一：临时设置（仅当前终端）**

CMD（命令提示符）：
```cmd
set DEEPSEEK_API_KEY=sk-你的key
```

PowerShell：
```powershell
$env:DEEPSEEK_API_KEY = "sk-你的key"
```

Linux / macOS：
```bash
export DEEPSEEK_API_KEY=sk-你的key
```

设置后**在同一个终端**里再运行 `streamlit run src/ui/streamlit_app.py`。

**方式二：永久设置（推荐，一劳永逸）**

CMD / PowerShell 都可用：
```cmd
setx DEEPSEEK_API_KEY "sk-你的key"
```
> `setx` 只对**新开的终端**生效，设置后请关闭当前终端、重新打开再运行。

**验证是否设置成功：**

```cmd
echo %DEEPSEEK_API_KEY%          :: CMD 验证
```
```powershell
echo $env:DEEPSEEK_API_KEY       # PowerShell 验证
```

能打印出 `sk-...` 即成功；运行后日志会显示 `DEEPSEEK_API_KEY=已设置`。

**云端部署**：不用环境变量，在 Streamlit Cloud 的 **Secrets 面板**里填（见 5.6 节）。

**其它可调参数**（`config.py` 里的常量，按需修改）：

```python
TOP_K_FIRST = 50          # FAISS 粗筛候选数（越大召回越全、越慢）
TOP_K_FINAL = 15          # Rerank 精排最终返回数
BM25_K = 50               # BM25 关键词候选数
ENSEMBLE_WEIGHTS = [0.5, 0.5]  # [语义检索, 关键词检索] 融合权重，和为 1
CHUNK_SIZE = 2500         # 文档切块大小（越大块越完整、检索精度略降）
TEMPERATURE = 0.3         # 答案生成温度（0=确定，1=随机）

# 生成 LLM（换模型/换 API 供应商见 4.5 节）
LLM_BASE_URL = "https://api.deepseek.com/v1"   # OpenAI 兼容接口地址
LLM_MODEL    = "deepseek-chat"                 # 模型名

# 查询重写
QUERY_REWRITE_ENABLED = True    # 是否开启查询重写
QUERY_REWRITE_COUNT = 2         # 每个问题改写成几个查询（调小可降低回答等待时间）
QUERY_REWRITE_TOP_K = 20        # 每个改写查询的候选数（2×20=40 → 去重 → 精排取15）
```

### 4.3 放入文档

把需要检索的文档放进 `docs/` 文件夹即可。支持格式：

| 格式 | 说明 |
| --- | --- |
| `.pdf` | 常用（推荐） |
| `.docx` | Word 新版格式 |
| `.txt` | 纯文本 |
| `.doc` / `.md` / 其他 | **不支持**，会被自动跳过 |

**注意事项：**
- 直接放在 `docs/` **顶层**即可，**不支持子文件夹**（只扫第一层）
- 文件名含中文没问题（限制的是**索引保存路径**，见 4.4 节）
- 一个文档对应一个"来源"，检索时按文件名 + 页码标注

当前知识库：`docs/GaussDB V2.0-25.860.0 参考指南(for 华为云Stack 8.6.0) 01.pdf`（338 页）。

### 4.4 构建索引

```bash
python scripts/build_index.py
```

成功输出示例：

```text
📁 扫描文档目录：F:\智能值班助手Agent\docs
📄 正在加载：GaussDB V2.0-25.860.0 参考指南(...).pdf
   ✅ 加载成功，共 338 页/段
✅ 切分为 370 个文本块
✅ 向量化完成，共 370 个向量
✅ 索引已保存
🎉 索引构建完成！
```

> **重要：每次新增或修改 `docs/` 中的文档后，必须重新运行此命令**，否则 FAISS 检索仍是旧数据。

**两份索引（务必理解）：**

| 位置 | 作用 | 何时更新 |
| --- | --- | --- |
| `F:/faiss_index_data` | **本机**运行时加载的索引（中文路径自动落到这里） | 每次 `build_index.py` |
| `data/faiss_index/` | **云端**加载的索引（随仓库携带） | 只有跑 `--sync-repo` 才会更新 |

**仅本地演示**：跑 `python scripts/build_index.py` 即可，然后 Web 界面点「🔄 重新加载」（或重启）。

**要同步到云端**（加文档后云端也要能检索到）：

```bash
python scripts/build_index.py --sync-repo     # 构建 + 一键复制到仓库内 data/faiss_index
git add docs/ data/faiss_index                # 把新文档 + 新索引一起提交
git commit -m "知识库：新增 xxx 文档"
git push                                      # 推送 main 后云端自动重新部署
```

### 4.5 换 API / 换 LLM（不改代码）

生成 LLM 的 `LLM_BASE_URL`（接口地址）与 `LLM_MODEL`（模型名）都集中在 `config.py`，支持环境变量覆盖。换模型只需改这两个值 + 重启，**不用改任何 `.py` 代码**。

**场景一：只换 API Key（同一供应商）**

- 本地：设环境变量（见 4.2 节）→ 重启。
- 云端：改 Secrets 面板的 `DEEPSEEK_API_KEY` → Reboot。

**场景二：换 LLM 供应商**

改 `config.py` 顶部的两个值（或设同名环境变量）：

```python
LLM_BASE_URL = "https://api.deepseek.com/v1"  # 改成新供应商的 OpenAI 兼容地址
LLM_MODEL    = "deepseek-chat"                # 改成模型名
```

常见 OpenAI 兼容接口示例（具体以各家官方文档为准）：

| 供应商 | base_url | model 示例 |
| --- | --- | --- |
| DeepSeek（默认） | `https://api.deepseek.com/v1` | `deepseek-chat` |
| 通义千问（阿里 DashScope） | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| 文心一言（百度千帆） | `https://qianfan.baidubce.com/v2` | `ernie-4.0-8k` |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4` |

> 只支持 **OpenAI 兼容接口**（项目用 `ChatOpenAI` 按该协议调用）。API Key 也要换成新供应商的 key（本地设 `DEEPSEEK_API_KEY` 环境变量、云端改 Secrets）。

**场景三：云端换模型**

Streamlit Cloud 的 Secrets 面板加两行（改完 Reboot 生效）：

```toml
LLM_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
LLM_MODEL    = "qwen-plus"
```

**附注**：换**嵌入模型**（`EMBEDDING_MODEL`）或 **Rerank 模型**（`RERANK_MODEL`）不用改 API Key；但嵌入模型变化后**必须重建索引**（4.4 节，用 `--sync-repo` 同步云端），否则新旧向量维度不一致会报错。

---

## 五、运行方式

### 5.1 Web 界面（推荐）`src/ui/streamlit_app.py`

```bash
streamlit run src/ui/streamlit_app.py
```

浏览器打开 `http://localhost:8501`。启动时自动加载 FAISS 索引、Rerank 模型、重建 BM25 索引，首次加载约需 30~60 秒。

**界面功能：**

| 功能 | 使用方式 |
| --- | --- |
| 提问 | 底部输入框直接输入问题（如"值班遇到 DBS.200026 怎么处理"） |
| 流式输出 | 走检索/工具路径时逐字实时渲染 |
| 多轮对话 | 自动记住上下文，可直接追问 |
| 查看来源 | 回答下方 `📄 查看引用来源` 展开引用文档 |
| 调试信息 | 侧边栏 `🧠 调试信息` 可看查询重写结果、来源数量、历史消息数 |
| 重新加载 | 侧边栏 `🔄 重新加载`（改文档/模型后点此重载） |
| 清空对话 | 侧边栏 `🗑️ 清空对话` |

**试问答（验证三大能力）：**

- 问题分类 + 解决方案：`错误码 DBS.200026 表示什么？怎么处理？`（应返回错误描述/原因/建议）
- 操作指南检索：`如何设置 GaussDB 的备份策略？`
- 日志路径查询：`GaussDB 的日志文件在哪里？`

### 5.2 API 服务 `工程化和部署/api.py`

```bash
python 工程化和部署/api.py
```

访问 `http://localhost:8000/docs` 查看 Swagger 接口文档。

**接口一览：**

```bash
# 服务信息
GET  http://localhost:8000/

# 健康检查
GET  http://localhost:8000/health
# → {"status": "healthy", "agent_loaded": true}

# 问答接口
POST http://localhost:8000/ask
Content-Type: application/json

{"question": "值班时遇到 DBS.200026 错误怎么处理？"}
```

**返回示例：**

```json
{
  "answer": "DBS.200026 错误表示数据库连接超时...（含错误描述、可能原因、解决建议、参考来源）",
  "sources": ["GaussDB V2.0-25.860.0 参考指南(...).pdf（第120页）"],
  "used_tool": true
}
```

### 5.3 查询重写端到端测试 `scripts/test_rewrite.py`

```bash
python scripts/test_rewrite.py
```

CLI 方式测试：提问"值班时遇到错误码 DBS.200026 怎么处理？"，打印改写后的查询、来源、prompt 前 200 字，便于脱离 Web 排查检索链路。

### 5.4 检索/回答评测 `scripts/evaluate.py`

```bash
python scripts/evaluate.py                 # 检索命中率模式（快）
python scripts/evaluate.py --mode full     # 端到端：跑完整 Agent + 生成答案
```

三变体消融评测，对照 [scripts/eval_set.json](scripts/eval_set.json) 的 40 题（错误码 15 / 日志路径 10 / 操作指南 10 / 路由边界 5）：

- `baseline`：查询重写开 + BM25 开（当前线上配置）
- `no_rewrite`：关查询重写 → 评估查询重写贡献
- `no_bm25`：关 BM25 → 评估关键词召回贡献

结果增量写入 `data/eval_results.json`，中断可续跑；需要 `DEEPSEEK_API_KEY`。可用 `--limit` 先小规模冒烟，`--variants` 只跑部分变体。

### 5.5 Streamlit 简单版 `工程化和部署/app.py`

```bash
streamlit run 工程化和部署/app.py
```

纯 RAG 链模式（FAISS 粗筛 → Rerank → DeepSeek），**无 Agent、无多工具、无查询重写**，适合快速验证检索效果。

### 5.6 部署到 Streamlit Cloud（演示/验收）

适合：把应用部署成一个公网可访问的 Demo，供导师/评审远程体验。项目已内置部署就绪支持（`config.py` 环境变量化、索引入库 `data/faiss_index/`）。

#### 部署完成后的运行方式（先说清楚它跑在哪）

部署完成后，**应用完全运行在云端，与本机无关**：

```text
代码仓库（GitHub）──自动部署──▶ Streamlit Cloud（境外服务器）
                                    │
                    首次启动时从 huggingface.co 下载两个模型到云端
                                    │
     用户访问公网 URL 提问 ─▶ 云端检索知识库 ─▶ 云端调用 DeepSeek API ─▶ 返回答案
```

| 环节 | 位置 |
| --- | --- |
| 代码 | GitHub 仓库（如 `xxg888/duty-assistant`），改动推送 `main` 即自动重新部署 |
| App 运行 | Streamlit Cloud 服务器（境外），**本机关机也不影响访问** |
| 嵌入 / Rerank 模型 | 首次启动时下载到云端服务器缓存 |
| 知识库索引 | 随仓库携带的 `data/faiss_index/` |
| 问答生成 | 云端运行时调用 DeepSeek API |
| 公网入口 | 形如 `https://duty-assistant-xxx.streamlit.app`，拿到链接即可访问（验收入口） |

> ⚠️ 这是**公网演示 Demo**，用于验收/演示，不是部署在华为云 HCS 生产环境。演示前可用手机流量（不要连同一 WiFi）打开公网链接，确认外网可达。

> 🔒 **访问与权限说明**：公网链接是**只读使用入口**——访客只能提问，**无法修改**代码、配置、模型或部署。"Manage app" 管理按钮只对**已登录的 owner** 显示；其他人（匿名访客 / 只读协作者）的 Reboot / Settings / Delete 均不可用。真正的风险是访客提问会消耗你的 DeepSeek API 额度，因此链接请只发给评审人员。

#### 第 1 步：推送到 GitHub（用私有仓库）

> ⚠️ 若 GaussDB 参考指南涉密，务必用**私有仓库**，且 Demo 链接只发给评审、不要公开。

```bash
# 在 GitHub 新建私有仓库（ASCII 名，如 duty-assistant）
git init
git add .
git commit -m "智能值班助手 Agent"
git remote add origin https://github.com/你的用户名/duty-assistant.git
git push -u origin main
```

#### 第 2 步：在 Streamlit Cloud 部署

1. 打开 `https://share.streamlit.io`，用 GitHub 账号登录
2. **New app** → 连接 GitHub → 选择仓库和分支
3. **Main file path** 填 `src/ui/streamlit_app.py`
4. 添加 **Secrets**（环境变量）：

```toml
DEEPSEEK_API_KEY = "sk-你的真实密钥"
HF_OFFLINE = "0"                             # 云端联网下载模型
# FAISS_INDEX_PATH = "/app/data/faiss_index"  # 可省略：代码自动使用仓库内 data/faiss_index（已验证）
# HF_ENDPOINT = "https://hf-mirror.com"       # 默认官方源；国内本地联网下载可改镜像
# LLM_BASE_URL = "https://api.deepseek.com/v1" # 换 LLM 供应商时填（见 4.5 节）
# LLM_MODEL = "deepseek-chat"                 # 换 LLM 供应商时填（见 4.5 节）
```

5. 点击 **Deploy** → 首次启动会下载模型（约 500MB+90MB），需等几分钟
6. 访问公网地址（形如 `https://duty-assistant-xxx.streamlit.app`）验证问答

**注意事项：**

- 免费版内存约 1GB，模型加载可能内存不足（OOM）；若崩溃需接受或后续优化
- 空闲休眠后唤醒会重新加载模型，需等待 1~3 分钟
- 免费版无内置登录，拿到链接的人都能提问并消耗 DeepSeek API 额度，请只把链接发给评审人员

**模型下载失败怎么排障：**

打开 App → **Manage app → Logs**，看最前面的几行：

- `[启动诊断] HF_OFFLINE=0 HF_ENDPOINT=... DEEPSEEK_API_KEY=已设置` — 确认 Secrets 是否注入；`HF_OFFLINE` 必须是 `0` 才允许联网下载模型
- `[网络诊断] https://huggingface.co → ✅/❌` — 确认下载源是否可达；默认官方源 `huggingface.co`，国内本地联网下载可设 `HF_ENDPOINT="https://hf-mirror.com"`

常见失败：`HF_OFFLINE` 未设或为 `1` → 模型只能从本地缓存加载；`HF_ENDPOINT` 指向境外连不上的镜像 → 换官方源。

---

## 六、功能详解

### 6.1 查询重写（改进口语化问题的召回）

- 开关：`QUERY_REWRITE_ENABLED`（默认开）
- 流程：LLM 把口语问题改写为 `QUERY_REWRITE_COUNT` 个检索友好查询（提取错误码、关键词、补充同义词）→ 每个查询各检索 `QUERY_REWRITE_TOP_K` 个候选 → 按来源**去重合并** → Rerank 精排取 15 个
- 例：`"那个数据库报错怎么办"` → 改写为多个变体，提高召回
- 任何重写失败会自动回退为原始问题，不影响主流程

### 6.2 多路召回（语义 + 关键词互补）

| 路 | 技术 | 优势 |
| --- | --- | --- |
| 语义检索 | FAISS + MiniLM 嵌入 | 理解含义，召回语义相近内容 |
| 关键词检索 | BM25 + jieba 中文分词 | 精确匹配错误码、日志路径、参数名（如 `DBS.200026`） |
| 融合 | EnsembleRetriever（RRF） | 权重 `[0.5, 0.5]`，两路结果融合去重 |

### 6.3 多轮对话

- 每轮对话完整保留在 `state["messages"]`，下一轮作为历史喂给 LLM
- 走工具检索后的真实回答也会写回历史，追问有上下文
- 「清空对话」可重置会话

### 6.4 自动化问题分类与方案推荐

生成 prompt 内置分类指令：对问题分类（**错误码查询 / 日志路径查询 / 操作指南查询**）。

- 错误码类 → 必须返回：**错误描述、可能原因、解决建议**
- 多文档 → 按相关性从高到低排列，结尾附参考来源

---

## 七、常见问题排查

### Q1：启动报"未找到向量库"

先运行 `python scripts/build_index.py` 构建索引。确保 `config.py` 中 `INDEX_SAVE_PATH` 路径**不含中文**（FAISS 后端不支持中文路径）。

### Q2：模型加载很慢 / 首次卡很久

首次运行需从 HuggingFace 下载模型（约 500MB），之后走本地缓存。若想离线运行，确保模型已缓存且 `config.py` 已设置 `HF_HUB_OFFLINE=1`。

### Q3：检索不到中文内容 / BM25 召回效果差

确认已安装 `jieba`。本项目已为 BM25 显式配置中文分词器（`chinese_tokenizer`），但该依赖缺失时 BM25 会退化为按空格分词、中文检索失效。

### Q4：BM25 报 "No module named rank_bm25"

`rank_bm25` 已列入 requirements.txt；若全新环境仍缺失，手动补装即可：

```bash
pip install rank_bm25
```

### Q5：走工具的问题 API 返回空答案

当前 `api.py` 已在 `/ask` 内用 prompt 补全答案（此问题已修复）。若仍有异常，检查 DeepSeek API Key 与网络。

### Q6：DeepSeek 返回 503 / 429

DeepSeek 服务繁忙或限流，稍等几秒重试。

### Q7：想换其他 LLM

生成 LLM 的接口地址与模型名已收敛到 `config.py` 的 `LLM_BASE_URL` / `LLM_MODEL`（也支持环境变量覆盖），换任何 OpenAI 兼容接口只需改这两个值 + 重启，不用改代码。完整操作（本地 / 云端）见 **§4.5**。

### Q8：改了 docs/ 文档但回答没变化

必须重新构建索引：`python scripts/build_index.py`，然后在 Web 界面点「🔄 重新加载」。

### Q9：Windows 终端打印 emoji 乱码/报错

控制台默认 GBK 编码无法输出 emoji。请用 `python` 运行时设置 `PYTHONIOENCODING=utf-8`，或使用本项目的 Streamlit/FastAPI 入口（已内置 UTF-8 处理）。

### Q10：启动报缺失依赖

```bash
pip install -r requirements.txt
```

并确认 `jieba`、`fastapi`、`uvicorn` 已装（均已列入 requirements.txt）。

---

## 八、技术栈

| 组件 | 技术 |
| --- | --- |
| LLM | DeepSeek（deepseek-chat） |
| 嵌入模型 | sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2（384 维） |
| Rerank | cross-encoder/ms-marco-MiniLM-L-6-v2 |
| 向量库 | FAISS |
| 关键词检索 | BM25（rank_bm25 + jieba 中文分词） |
| Agent 框架 | LangGraph |
| 文档加载 | pypdf / docx2txt |
| Web 界面 | Streamlit |
| API 服务 | FastAPI + uvicorn |

---

## 九、注意事项与限制

1. **`INDEX_SAVE_PATH` 必须为纯 ASCII 路径**（当前 `F:/faiss_index_data`）。
2. **DeepSeek API Key**：`config.py` 只从环境变量 `DEEPSEEK_API_KEY` 读取（本地 CMD 用 `set`、PowerShell 用 `$env:` 设置，云端用 Streamlit Secrets 注入，详见 4.2 节），**代码中不保存任何明文 Key**；未设置会打印警告且无法调用 DeepSeek。
3. **`工程化和部署/app.py` 是简化版**：无 Agent、无查询重写、无多轮记忆，与主版本（`src/ui/streamlit_app.py`）功能不同。
4. **BM25 索引每次启动时基于 `docs/` 重建**（非预构建），文档很多时首次启动会略慢。
5. **Git 提交**：项目已配置 `.gitignore`（自动排除 `__pycache__/`、`.env`、虚拟环境、FAISS 索引）；若手动打 zip，请先删除 `__pycache__` 目录。
