# 实习总结汇报

## 面向华为云 HCS 的智能值班助手 Agent

> 实习单位：中国工商银行数据中心（系统1部）· AI 应用开发岗位
> 实习时间：2026 年 7 月 — 2026 年 8 月
> 汇报人：周翔

---

## 一、实习概况

本次实习在中国工商银行数据中心系统1部从事 AI 应用开发工作。实习期间，我独立负责并完成了「智能值班助手 Agent」项目：基于 **RAG（检索增强生成）+ LangGraph** 技术栈，构建了一个面向华为云 HCS（Huawei Cloud Stack）的智能值班问答系统。该系统能将值班人员的常见问题（错误码处理、日志路径查询、操作指南检索）自动分类，从 GaussDB 知识库中检索相关文档，并生成带引用来源的解决方案，显著提升了值班问题的排查效率。

项目自需求分析、技术选型、功能开发、性能评测到云端部署交付全程独立完成，通过了部门验收演示，目前以 Web 应用与 API 服务两种形态提供能力。

## 二、项目背景与目标

**业务痛点。** 数据中心值班人员在日常运维中高频遇到的问题集中在三类：遇到报错时需查询错误码的含义与处置方法、排查问题时需定位日志文件路径、执行操作时需查阅操作指南。传统模式下，这些问题依赖人工翻阅海量技术文档（本项目知识库原始资料即达 338 页），检索效率低、定位慢，且对值班人员的经验依赖度高。

**项目目标。** 构建一个智能问答助手，实现三个核心能力：

1. **自动分类**：由大模型自主判断问题类型（错误处理 / 日志分析 / 操作指南），而非依赖固定规则；
2. **精准检索**：针对口语化、模糊的长尾问题，也能从知识库中召回正确的解决方案；
3. **可解释输出**：生成答案时附带引用来源，值班人员可回溯核验，避免"AI 幻觉"误导运维操作。

**技术选型理由。** 采用 RAG + LangGraph + DeepSeek 的组合：RAG 保证回答基于真实知识库、可溯源；LangGraph 提供灵活的多节点 Agent 流程编排，让大模型能够自主决策是否调用检索工具；DeepSeek 作为大模型底座，在成本与效果上取得平衡，且 API 为 OpenAI 兼容格式，便于替换。

## 三、总体方案与技术架构

系统整体为「前端界面 → Agent 编排层 → 检索层 → 大模型生成」的分层架构：

```text
用户提问
   │
   ▼
Streamlit Web 界面 / FastAPI 接口
   │
   ▼
LangGraph Agent（四个核心节点）
   analyze_question → rewrite_query → execute_tools → generate_answer
   │                                      │
   │                      ┌────────────────┘
   ▼                      ▼
DeepSeek 生成回答    RAG 检索层：FAISS 语义检索 + BM25 关键词检索
                         → RRF 融合 → CrossEncoder 精排
   │
   ▼
带引用来源的解决方案（Web / API 返回）
```

其中 LangGraph Agent 的四个核心节点分工如下：

| 节点 | 职责 |
| --- | --- |
| `analyze_question` | LLM 自主决策问题是否走工具（检索/天气/时间），还是直接闲聊回答 |
| `rewrite_query` | 将口语化问题改写为多个检索友好的查询，提升召回率 |
| `execute_tools` | 执行多路检索、调用外部工具，聚合上下文 |
| `generate_answer` | 基于检索结果组装 prompt，交给大模型生成最终答案 |

## 四、关键技术实现

### 4.1 Agent 流程设计（LangGraph）

传统 RAG 是"提问 → 检索 → 生成"的单次流水线，无法处理"该不该检索"的决策与多轮交互。本系统基于 LangGraph 将其升级为**带状态的多节点 Agent**：大模型先分析问题性质（是否需要工具），需要检索时再走重写→召回→精排链路，最后生成答案。相比单次流水线，这个设计能：

- **避免无效检索**：闲聊/常识类问题不触发检索，节省资源、降低延迟；
- **支持多轮对话**：通过状态对象传递历史消息，上下文可延续；
- **流程可观测**：每一步的中间产物（改写查询、召回文档、引用来源）都可暴露给前端展示，便于排查问题。

### 4.2 查询重写（Query Rewrite）

值班人员的提问往往口语化、不完整（如"数据库起不来怎么办"），直接拿去检索命中率低。我设计了**查询重写策略**：由大模型从问题中提取**错误码**等关键实体，并补充**同义词/规范术语**，一次生成多个检索友好的查询（如将口语问题改写为包含错误码与官方术语的检索词），再对多个查询的结果合并去重。

该策略针对值班场景最典型的**模糊长尾问题**效果显著，是项目准确率提升的关键一环（量化数据见第五节）。

### 4.3 多路召回与精排

单一检索方式各有短板：纯语义检索（FAISS 向量检索）对专业名词、错误码这类字面匹配不敏感；纯关键词检索（BM25，jieba 中文分词）对语义相近但用词不同的表述无能为力。我构建了**多路召回 + 精排**的检索链路：

1. **召回**：FAISS 语义检索与 BM25 关键词检索并行，各自召回候选；
2. **融合**：对两路结果做 RRF（Reciprocal Rank Fusion）融合，综合排序；
3. **精排**：用 CrossEncoder 重排模型对融合后的候选二次精排，取最优结果。

知识库侧，我将 338 页的 GaussDB 参考文档切分为 370 个文本块（重叠切分避免边界信息丢失），并完成向量化索引构建。

### 4.4 评测体系（以数据驱动优化）

为保证优化不是"拍脑袋"，我搭建了一套可量化的评测体系：

- **评测集**：编写 40 道覆盖错误码（15 题）、日志路径（10 题）、操作指南（10 题）、路由边界（5 题）四类的标准评测题，每题标注标准答案关键词；
- **评测脚本**：支持"检索命中率"（快）与"端到端含回答"（慢）两种模式，结果增量落盘、可续跑；
- **消融实验**：对「查询重写」「BM25 多路召回」两个关键设计做开/关三变体对比，精确量化每个模块的贡献。

这套体系让每次调优都能用数据说话，也是项目可信度的核心支撑。

### 4.5 工程化与部署

项目按职责分层组织（配置层 / RAG 检索层 / Agent 层 / 界面层 / 部署层），提供两种使用形态：

- **Streamlit Web 界面**：对话式交互，展示查询重写结果、引用来源、调试信息；
- **FastAPI 服务**：标准 RESTful 接口，便于集成到其他系统。

部署采用 Streamlit Cloud，模型与索引按离线/在线双模式配置（本地离线用缓存、云端联网），代码与索引经 Git 版本管理随仓库发布，实现了"改文档 → 重建索引 → 一键同步"的持续更新流程，并通过了部门验收演示。

## 五、工作成果与量化数据

| 指标 | 结果 |
| --- | --- |
| 15 组问答调优 | 准确率由 **93.3% 提升至 100%** |
| 40 题评测集 · 基线（重写+BM25 全开） | 检索命中率 **35/35 = 100%** |
| 40 题评测集 · 仅关查询重写 | 31/35 = **88.6%**（下降 11.4 个百分点） |
| 40 题评测集 · 仅关 BM25 | 32/35 = **91.4%**（下降 8.6 个百分点） |
| 知识库构建 | 338 页文档 → 370 个文本块，全部向量化 |
| 交付 | Streamlit Cloud 云端部署，通过验收演示，可远程访问 |

消融实验的结论清晰验证了两个核心设计的价值：

- **查询重写贡献 +11.4pp**：关闭后 4 题失分覆盖错误码、日志路径、操作指南三类（如 L06 日志目录、O04 操作指令），证明口语化改写对各类模糊提问的召回都是刚需；
- **BM25 多路召回贡献 +8.6pp**：关闭后丢分集中在错误码类（专业名词语义检索不敏感），证明关键词路线的不可替代性；
- **部分错误码题目（如 E05/E12）需要两者同时开启才命中**，说明语义检索与关键词检索是协同关系、缺一不可。

## 六、遇到的问题与解决

实习过程中遇到并解决了多个真实工程问题，挑典型的三个记录如下：

**1. Windows 控制台打印 emoji 崩溃（编码问题）。**

- 现象：在 GBK 编码的 Windows 终端运行脚本时，打印含 `⚠️` 等 emoji 的日志直接报 `UnicodeEncodeError` 崩溃。
- 定位：配置文件在模块导入时就会执行打印，而此刻 stdout 尚未套上 UTF-8 包装，GBK 控制台遇到超范围字符即抛错。
- 修复：将提示信息中的 emoji 替换为纯文本，并在所有入口脚本统一设置 UTF-8 输出与 `line_buffering`，从根源上消除编码崩溃隐患。

**2. 长时间运行的评测脚本"看似卡死"。**

- 现象：评测跑批时进度长时间不刷新，误以为卡住。
- 定位：stdout 默认是块缓冲，print 的内容要攒够一定量才输出，导致进度"憋着不显示"。
- 修复：将标准输出改为行缓冲（`line_buffering=True`），进度逐行实时刷新，并支持断点续跑。

**3. FAISS 不支持中文路径。**

- 现象：向量索引保存到含中文的本地路径时构建失败。
- 定位：FAISS 的 C++ 后端不兼容非 ASCII 路径。
- 修复：索引改存到纯 ASCII 路径，并设计"环境变量 > 仓库内 ASCII 路径 > 本地 ASCII 路径"的优先级解析逻辑，本地与云端部署统一。

此外，通过查询重写调参（候选检索数由 3×30 降至 2×20），在保持准确率的前提下将检索与重排耗时减半，优化了回答等待体验。

## 七、个人收获与反思

**技术层面。** 本次实习让我第一次完整走通了 **LLM Agent 应用从 0 到 1 的全链路**：从 LangGraph 多节点流程编排，到 RAG 的检索增强、查询重写、多路召回与精排，再到以评测集驱动优化的方法论。特别是"消融实验"的实践，让我真正理解了如何用数据验证一个设计是否有效，而不是凭感觉优化。

**工程层面。** 项目全程采用模块化分层与文档化交付（使用手册、代码说明齐全），并使用 Git 进行版本管理、发布到云端持续迭代。这让我养成了"代码可读、改动可追溯、文档可交付"的工程习惯，也体会到工程化对项目长期维护的价值。

**业务层面。** 通过接触银行数据中心的运维场景，我理解了"高可靠、可解释、可追溯"在金融行业技术应用中的分量——一个给值班人员用的工具，答案必须能溯源、容不得幻觉，这直接影响了我对技术方案取舍的判断。

**不足与展望。** 项目仍有可改进空间：如评测集可进一步扩大覆盖、可引入更细粒度的答案质量评估、支持多知识库切换等。未来若能结合更多真实值班问题持续迭代，系统的实用性还能再上一个台阶。

## 八、总结

本次实习中，我从零独立完成了一个面向华为云 HCS 的智能值班问答助手：以 RAG + LangGraph 搭建了具备自动分类、精准检索、可解释输出能力的 Agent 系统，通过查询重写与多路召回的设计和 40 题评测集的消融验证，将问答准确率从 93.3% 提升到 100%，并完成了云端部署与验收交付。这段经历不仅让我建立了大模型应用的完整工程能力，更让我体会到"以数据驱动、以用户价值为导向"做技术的重要。感谢部门和带教老师在实习期间的指导，未来我将把这段积累运用到更深入的技术实践中。
